import os

os.remove("sample.txt") #os.remove() method is used to delete a file. It takes the file name as an argument and deletes the file from the file system. It is important to note that once a file is deleted, it cannot be recovered, so use this method with caution.